# Prevent execution of any script files inside the uploads directory.
# Uploaded images should only ever be served as static files.
<FilesMatch "\.(php|php3|php4|php5|phtml|pl|py|cgi|asp|aspx|sh)$">
    Require all denied
</FilesMatch>
Options -Indexes
